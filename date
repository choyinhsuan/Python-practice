import datetime

def days_diff(a: tuple[int, int, int], b: tuple[int, int, int]) -> int:
    # your code here
    dt1 = datetime.date(a[0], a[1], a[2])
    dt2 = datetime.date(b[0], b[1], b[2])
    
    return abs(dt1-dt2).days

